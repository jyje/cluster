apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hermes-agent.fullname" . }}-env
  labels:
    {{- include "hermes-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
